<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp" >

    <TextView
        android:id="@+id/redacteur_commentaire"
        android:textColor="@android:color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:text="Pseudo" />

    <TextView
        android:id="@+id/date_commentaire"
        android:textColor="@android:color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/redacteur_commentaire"
        android:layout_alignTop="@id/redacteur_commentaire"
        android:layout_marginLeft="10dp"
        android:text="(Il y a 10min)" />

    <TextView
        android:id="@+id/message_commentaire"
        android:textColor="@android:color/black"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_below="@+id/redacteur_commentaire"
        android:text="Blablablablablablablablablablablablablablabla" />

</RelativeLayout>